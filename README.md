# 🤖 Yapay Zeka Kod Yazarlığı Tespit Aracı

## 🎯 Proje Amacı

Bu dönem projesi, verilen bir kod parçasının (snippet) bir **İnsan Geliştirici** tarafından mı yoksa bir **Yapay Zeka (LLM)** asistanı tarafından mı yazıldığını belirlemek üzere tasarlanmış bir Makine Öğrenimi (ML) sınıflandırma modeli uygulamaktadır. Temel amaç, yapay zeka tarafından üretilen kodların yaygınlaşmasıyla ortaya çıkan otantiklik sorununa çözüm getirmektir.

---

## ✨ Temel Özellikler

* **Kod Girişi:** Web uygulaması arayüzündeki bir metin alanı aracılığıyla kod parçalarını kabul eder.
* **Çoklu Model Tahmini:** Proje gereksinimine uygun olarak, üç farklı ML algoritmasından (**Naive Bayes, SVM, Random Forest**) elde edilen oransal olasılık skorlarını (% Yapay Zeka / % İnsan) kullanıcıya sunar.
* **Özel Veri Seti:** Model, hazır veri seti kullanmama kısıtlamasına uyarak, sıfırdan oluşturulmuş 5.000 (2.500 İnsan, 2.500 Yapay Zeka) kod örneği üzerinde eğitilmiştir.
* **Yüksek Kaliteli Veri Ayrıştırma:** İnsan kodunu fonksiyon/sınıf seviyesinde parçalamak için **Python'ın AST (Abstract Syntax Tree) modülü** kullanılmıştır.

---

## 🗃️ Özel Veri Seti Oluşturma (5.000 Örnek)

Proje isterlerine uygun olarak, veri setimiz tamamen özel scriptler kullanılarak toplanmış ve etiketlenmiştir.

* **İnsan Yazımı Kod (2.500 Örnek):**
    * **Kaynak:** Popüler, açık kaynaklı **MIT Lisanslı GitHub depolarından** çekilmiştir.
    * **Toplama Yöntemi:** GitHub API kullanılarak depolardaki `.py` uzantılı dosyalar alınmış ve kod bloklarına (**fonksiyon/sınıf**) ayrılmıştır.
* **Yapay Zeka Yazımı Kod (2.500 Örnek):**
    * **Kaynak:** Gemini gibi güçlü bir Büyük Dil Modeli (LLM) kullanılarak üretilmiştir.
    * **Üretim Yöntemi:** Çeşitli algoritmalar, veri yapıları ve işlevsel görevleri kapsayan geniş bir prompt (istem) yelpazesi kullanılmıştır.

**Veri Formatı:** Tüm örnekler, kod içeriği, kaynak, lisans ve dil gibi meta verileri içeren JSON formatında kaydedilmiştir.

---

## ⚙️ Kullanılan Teknolojiler

* **Programlama Dili:** Python
* **Veri Toplama & Ön İşleme:** Python (`requests`, `ast`, `json`, `base64`)
* **Makine Öğrenimi (Modeller):** Scikit-learn (Naive Bayes, Support Vector Machines, Random Forest)

---

## 🚀 Kurulum ve Çalıştırma

### Ön Koşullar

1.  Python 3.x yüklü olmalıdır.
2.  Bir Sanal Ortam (`venv`) oluşturulmalıdır.
3.  GitHub Personal Access Token (PAT) gereklidir.

### Projeyi Başlatma Adımları

1.  **Bağımlılıkları Kurma:**
    ```bash
    pip install requests scikit-learn [Web Çerçevesi Adı, örn: flask]
    ```

2.  **Veri Toplama:** Proje klasöründeki `data_collector.py` scriptini çalıştırın. (Token'ınızın doğru ayarlandığından emin olun.)
    ```bash
    python data_collector.py
    ```


3.  **Model Eğitimi:** Model eğitim scriptini çalıştırın.
    ```bash
    python model_trainer.py
    ```

4.  **Web Uygulamasını Başlatma:** Ana uygulama dosyasını çalıştırın.
    ```bash
    python app.py  # veya main.py
    ```

---

## 🤝 Katkıda Bulunma ve Lisans

Bu proje, Makine Öğrenmesi dersi için bir dönem projesi olarak geliştirilmiştir.

**Lisans:** MIT Lisansı

---
---

# 🤖 AI Code Authorship Detector

### 🎯 Project Goal

This project implements a Machine Learning (ML) classification model designed to determine the authorship of a given code snippet. The primary goal is to distinguish between code written by a Human developer and code generated by a Large Language Model (LLM) such as Gemini, GPT, or Copilot.

---

### ✨ Features

* **Code Input:** Accepts code snippets via a text field in a web application.
* **Multi-Model Prediction:** Provides proportional likelihood scores (e.g., 65% AI / 35% Human) from **three distinct ML algorithms** (e.g., Naive Bayes, SVM, Random Forest).
* **Custom Data Collection:** The model is trained on a meticulously curated dataset of 5,000 code samples (2,500 Human, 2,500 AI), manually collected to comply with project requirements and avoid public dataset penalties.

---

### 🗃️ Custom Dataset Collection

To ensure the integrity and originality required by the project specifications, the dataset was collected and labeled entirely from scratch.

* **Human-Authored Code (2,500 samples):** Collected from popular, open-source **MIT-licensed GitHub repositories** using the GitHub API and parsed into meaningful code blocks (functions/classes) using Python's **Abstract Syntax Tree (AST)** module.
* **AI-Generated Code (2,500 samples):** Generated using a powerful LLM (e.g., Gemini) by prompting it with a diverse array of coding problems (algorithms, utility functions, bug fixes).

**Data Format:** All samples are stored in JSON format with metadata (code, source, license, language).

---

### ⚙️ Technology Stack

* **Programming Language:** Python
* **Data Collection & Preprocessing:** Python (`requests`, `ast`, `json`, `base64`)
* **Machine Learning (Models):** Scikit-learn (Naive Bayes, Support Vector Machines, Random Forest)

---

### 🚀 Getting Started

#### Prerequisites

1.  Python 3.x
2.  A virtual environment (`venv`)
3.  Install dependencies: `pip install requests scikit-learn [Your Web Framework]`

#### Running the Project

1.  **Data Collection:** Run the provided `data_collector.py` script to generate the 5,000 code samples (Ensure your GitHub Personal Access Token is configured).
3.  **Model Training:** Run the training script (`model_trainer.py`) to train the three ML classifiers.
4.  **Launch Web App:** Run the main application file to start the web service.

```bash
python app.py  # or main.py
